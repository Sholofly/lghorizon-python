install-local: python3 -m pip install -e .

tag:    git tag -a v0.1.2 -m "v0.1.2"
        git push origin v0.1.2

create sdist: python3 setup.py sdist
upload: python3 -m twine upload dist/lghorizon-0.1.3.tar.gz
